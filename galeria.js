class Allapot {
    constructor(aktNagyKepIndex, max) {
        this.aktNagyKepIndex = Number(aktNagyKepIndex);

        this.max = Number(max);
        console.log(this.aktNagyKepIndex, this.max);
    }

   
}
